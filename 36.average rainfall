#include <stdio.h>
int main() {
    float r[5], sum = 0;
    int i;
    for (i = 0; i < 5; i++) {
        scanf("%f", &r[i]);
        sum += r[i];
    }
    printf("Average = %.2f", sum / 5);
    return 0;
}
