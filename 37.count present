#include <stdio.h>
int main() {
    int i, a[5], count = 0;
    for (i = 0; i < 5; i++) {
        scanf("%d", &a[i]);
        if (a[i] == 1)
            count++;
    }
    printf("Present = %d", count);
    return 0;
}
