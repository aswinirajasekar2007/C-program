#include <stdio.h>
struct student {
    int id;
    char name[20];
};

int main() {
    struct student s[5];
    int n;

    printf("Enter number of records: ");
    scanf("%d",&n);

    printf("Total Records = %d", n);
    return 0;
}
