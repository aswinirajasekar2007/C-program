#include <stdio.h>
struct emp {
    int id;
    char name[20];
};

int main() {
    struct emp e[3]={{1,"A"},{2,"B"},{3,"C"}};
    int sid,i;

    scanf("%d",&sid);

    for(i=0;i<3;i++) {
        if(e[i].id==sid)
            printf("Name: %s",e[i].name);
    }
    return 0;
}
